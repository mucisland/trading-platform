#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

echo "[run_fast_checks] TODO: run formatter checks"
echo "[run_fast_checks] TODO: run linter"
echo "[run_fast_checks] TODO: run type checker"
echo "[run_fast_checks] TODO: run fast smoke/unit subset"
